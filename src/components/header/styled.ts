import styled from "styled-components";
import { device } from "@/styles/media";

export const HeaderWrapper = styled.div`
    @media ${device.mobile} {
        .header_logo_poseNsen{
            display: none;
        }
    }

   
`;
